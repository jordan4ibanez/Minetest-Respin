local test = true
if test == true then
	print(" look at the notes ")
	return
end

--[[

make mob spawn entities that spawn in the sky above player, then when touching a node add mob and remove self
]]--
